let enemy;
let person;
let gameBoard;
let playButton;

const timer = 0;
const points = 0;

let gameStart = false;
let gameLoop;
let gameEnd = false;


function startGame() {
  if (gameStart = true) {
    return "game has started";
  }
  else if (gameStart = false)  {
    return "game not started";
  }
}

startGame();

console.log (startGame());